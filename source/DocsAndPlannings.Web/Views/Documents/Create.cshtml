@model DocsAndPlannings.Web.ViewModels.Documents.DocumentEditorViewModel
@{
    ViewData["Title"] = "Create Document";
}

<div class="row">
    <div class="col-md-12">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <div>
                <h2><i class="bi bi-file-plus"></i> Create New Document</h2>
                <p class="text-muted">Create a new documentation page with markdown support</p>
            </div>
            <div>
                <a asp-action="Index" class="btn btn-secondary">
                    <i class="bi bi-arrow-left"></i> Back to List
                </a>
            </div>
        </div>
    </div>
</div>

<div class="row">
    <div class="col-md-12">
        <div class="card">
            <div class="card-body">
                <form asp-action="Create" method="post">
                    @Html.AntiForgeryToken()
                    <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>

                    <div class="row mb-3">
                        <div class="col-md-8">
                            <label asp-for="Title" class="form-label"></label>
                            <input asp-for="Title" class="form-control" placeholder="Enter document title..." autofocus />
                            <span asp-validation-for="Title" class="text-danger"></span>
                        </div>
                        <div class="col-md-4">
                            <label asp-for="ParentDocumentId" class="form-label"></label>
                            <select asp-for="ParentDocumentId" class="form-select">
                                <option value="">-- None (Root Document) --</option>
                                @foreach (var doc in Model.AvailableParentDocuments)
                                {
                                    <option value="@doc.Id">@doc.Title</option>
                                }
                            </select>
                            <span asp-validation-for="ParentDocumentId" class="text-danger"></span>
                        </div>
                    </div>

                    <div class="mb-3">
                        <label asp-for="Content" class="form-label"></label>
                        <small class="text-muted">(Markdown supported)</small>
                        <textarea asp-for="Content" class="form-control font-monospace" rows="20" placeholder="Enter markdown content...

# Example Heading

This is a paragraph with **bold** and *italic* text.

- List item 1
- List item 2

```code
Code block example
```"></textarea>
                        <span asp-validation-for="Content" class="text-danger"></span>
                    </div>

                    <div class="row mb-3">
                        <div class="col-md-6">
                            <label asp-for="SelectedTagIds" class="form-label"></label>
                            <select asp-for="SelectedTagIds" class="form-select" multiple size="5">
                                @foreach (var tag in Model.AvailableTags)
                                {
                                    <option value="@tag.Id">@tag.Name</option>
                                }
                            </select>
                            <small class="text-muted">Hold Ctrl to select multiple tags</small>
                        </div>
                        <div class="col-md-6">
                            <div class="form-check mt-4">
                                <input asp-for="IsPublished" class="form-check-input" type="checkbox" />
                                <label asp-for="IsPublished" class="form-check-label"></label>
                                <small class="text-muted d-block">Published documents are visible to all users</small>
                            </div>
                        </div>
                    </div>

                    <div class="mt-4">
                        <button type="submit" class="btn btn-primary btn-lg">
                            <i class="bi bi-save"></i> Create Document
                        </button>
                        <a asp-action="Index" class="btn btn-secondary btn-lg">
                            <i class="bi bi-x-circle"></i> Cancel
                        </a>
                    </div>
                </form>
            </div>
        </div>

        @* Markdown Reference Card *@
        <div class="card mt-3">
            <div class="card-header">
                <h5 class="mb-0"><i class="bi bi-info-circle"></i> Markdown Quick Reference</h5>
            </div>
            <div class="card-body">
                <div class="row">
                    <div class="col-md-6">
                        <h6>Formatting</h6>
                        <ul class="list-unstyled font-monospace small">
                            <li># Heading 1</li>
                            <li>## Heading 2</li>
                            <li>**bold text**</li>
                            <li>*italic text*</li>
                            <li>`inline code`</li>
                        </ul>
                    </div>
                    <div class="col-md-6">
                        <h6>Lists & Links</h6>
                        <ul class="list-unstyled font-monospace small">
                            <li>- Bullet list item</li>
                            <li>1. Numbered list item</li>
                            <li>[Link text](URL)</li>
                            <li>![Image alt](image-url)</li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script>
        // Auto-expand textarea as content grows
        document.addEventListener('DOMContentLoaded', function() {
            const textarea = document.querySelector('textarea[name="Content"]');
            if (textarea) {
                textarea.addEventListener('input', function() {
                    this.style.height = 'auto';
                    this.style.height = (this.scrollHeight) + 'px';
                });
            }
        });
    </script>
}
